# Story: Setup Advanced Tooling (FVM, DCM, Lints)

**Story Point Estimate:** 3
**Priority:** Medium

## Description
Improve developer experience and code quality.

## Requirements
1.  **FVM:** Initialize `fvm` for consistent Flutter versioning.
2.  **DCM:** Setup Dart Code Metrics (if license available) or strict analysis.
3.  **Linters:**
    *   Add `pyramid_lint`, `custom_lint`, `leancode_lint`.
    *   Enable `use_text_editing_controller` rule (Pyramid).
4.  **Shorthand:** Enable and enforce Dart shorthand syntax (e.g. `Function()?`) and other concise patterns to save LOC.
