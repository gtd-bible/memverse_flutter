class FeatureFlags {
  // TODO: use dart define or firebease remote config for some feature flags
  // but this oen is really just to not lose the UI from stitch
  static const bool ratingsIsReady = false;
  static const bool addVerseIsReady = false;
}
