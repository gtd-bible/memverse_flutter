import 'package:flutter/material.dart';

class HomeTab extends StatelessWidget {
  const HomeTab({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text("Home Tab Placeholder"));
}

class VerseTextQuizScreen extends StatelessWidget {
  const VerseTextQuizScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text("Verse Text Quiz Placeholder"));
}

class MemversePage extends StatelessWidget {
  const MemversePage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text("Memverse Page Placeholder"));
}

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text("Settings Screen Placeholder"));
}
