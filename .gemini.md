# Gemini AI Instructions

## 🔴 CRITICAL: Read This First
**Before making ANY code changes, read `llm_notes.md` in the project root.**

This file contains critical context about:
- Authentication (OAuth API, NOT Firebase Auth)
- Database (Sembast, NOT Isar)
- Testing philosophy
- Common mistakes to avoid

## Primary Rule
**Code must compile after EVERY prompt response.**
Run `flutter analyze lib` before completing your response.

## Quick Reference
- Auth: Custom OAuth API (see `lib/src/features/auth/data/real_auth_service.dart`)
- Database: Sembast via `DatabaseRepository` interface
- Firebase: Analytics/Crashlytics ONLY (not for auth)
- Tests: BDD-focused, readable, minimal mocking

## When in Doubt
1. Check `llm_notes.md`
2. Ask the user
3. Don't break compilation
